/** `index` — the third of the three collision-name examples phase-04 §22
 * names (`handler`, `index`, `render`); also benign, present for realism. */
export function index(items: readonly string[]): string {
  return items.join(", ");
}
