node_modules
.next
dist
build
coverage
.turbo
.env*
apps/worker/tests/fixtures/parsing
apps/worker/tests/fixtures/graph-repo
# pnpm-managed, not hand-formatted — prettier's own YAML formatting disagrees with
# pnpm's own lockfile writer (quote style, key ordering) and reformatting it produces a
# multi-thousand-line diff with no real content change, every time either tool touches it.
pnpm-lock.yaml
# Generated by prisma generate — @repo/db's own build step re-creates it; reformatting a
# generated artifact this large only fights the generator on every regeneration.
packages/db/src/generated
